@using stortromme.Data

@if (Page.ContentType == ContentType.Text)
{
    <p>@Page.Content</p>
}
else if (Page.ContentType == ContentType.Image)
{
    <Canvas DisplayData=@Page.Content @ref="canvas"></Canvas>
}

@code
{
    [Parameter]
    public Page Page { get; set; }

    private Canvas canvas;
}